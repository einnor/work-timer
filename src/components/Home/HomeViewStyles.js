import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
  },
  welcomeHeader: {
    textAlign: 'center',
    fontSize: 40,
    color: '#000000',
    marginTop: 50,
  },
});

export default styles;